//module for organize the properties for printing
import house from "../scenes/house"; //should import the root scene here

// var scenes = {};

// var generateScene = function (scene) {
//   return { root: scene, children: scene.getChildScenes() };
// };

// scenes = generateScene(house);

export { house };
